COE67-222 Operating Systems - Lab 07 Report
Name: Takdanai Niyomsap
Student ID: 67122382

1. The Convoy Effect (Phase 2):
In the FCFS output of `cpu_scheduler.py`, what is the "Convoy Effect"? How did the presence of Process P1 affect the waiting times of P2 and P3?
ANS : สำหรับ Convoy Effect คือปรากฏการณ์ที่ Process ที่ใช้เวลานานทำให้ Process ที่ใช้เวลาสั้นต้องรอนาน ในการทดสอบของผม P1 ใช้เวลา CPU 10 หน่วย ทำให้ P2 ต้องรอ 10 หน่วย และ P3 ต้องรอ 12 หน่วย ส่งผลให้ค่าเฉลี่ย Waiting Time ของ FCFS เป็น 7.33 หน่วย
2. Choosing the Time Quantum:
In Round Robin, the Time Quantum is critical.
- What happens if the OS sets the Time Quantum extremely large (e.g., Quantum = 100)? Which algorithm does RR start to behave like?
- What happens if the OS sets the Time Quantum extremely small (e.g., Quantum = 0.0001)? (Hint: Think about Context Switching overhead).
ANS :  Time Quantum ใหญ่มาก 100 แต่ละ Process จะสามารถทำงานได้นานโดยไม่ถูกขัดจังหวะ ทำให้ Round Robin มีพฤติกรรมคล้าย FCFS 
 Time Quantum เล็กมาก 0.0001 ระบบจะเกิด Context Switching บ่อยมาก ทำให้เกิด Overhead สูง และประสิทธิภาพของระบบลดลง 
แต่จากการทดสอบของผมเมื่อ Quantum = 3 พบว่า P1 ถูกแบ่งการทำงานออกเป็นหลายรอบ ขณะที่ P2 และ P3 สามารถทำงานและเสร็จได้ก่อน จึงทำให้ Average Waiting Time ของ RR = 4.33 ซึ่งต่ำกว่า FCFS ที่ 7.33


3. AI Context:
In Phase 3, we used a Round Robin approach for generating tokens in an LLM. Explain why iteration-level Round Robin scheduling prevents long user queries from starving short queries, and how it improves perceived latency (Time To First Token / TTFT) for interactive AI applications.
ANS : สำหรับ Round Robin จะสร้าง Token ให้แต่ละ User สลับกันทีละรอบ แทนที่จะให้คำขอที่ยาวทำงานจนเสร็จก่อน จากการทดสอบ User_B_Math ต้องการเพียง 2 Tokens และทำงานเสร็จก่อน ในขณะที่ User_A_Essay ต้องการ 10 Tokens
วิธีนี้ช่วยป้องกันไม่ให้คำขอสั้นต้องรอคำขอยาว (Starvation) และช่วยให้ผู้ใช้ได้รับ Token แรกเร็วขึ้น (ลด TTFT) ทำให้ AI ตอบสนองได้เร็วและรู้สึกว่าใช้งานได้ลื่นไหลมากขึ้น
